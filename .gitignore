.cache/

# Fixture build/toolchain artifacts (fixtures/ ships only real source —
# these are regenerated by each fixture's own ./check). bun.lock and
# Cargo.lock ARE committed (repo-birth's own lockfile-committed convention).
fixtures/rust-hello/target/
fixtures/python-hello/.pytest_cache/
fixtures/python-hello/.ruff_cache/
fixtures/python-hello/**/__pycache__/
fixtures/ts-hello/node_modules/

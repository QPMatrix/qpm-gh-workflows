# syntax=docker/dockerfile:1
# Minimal fixture image for ts-gate.yml's self-test `image` job — proves a
# Dockerfile in a caller repo actually gets built, nothing more.
FROM oven/bun:1.4.0
COPY . /src

# syntax=docker/dockerfile:1
# Minimal fixture image for go-gate.yml's self-test `image` job — proves a
# Dockerfile in a caller repo actually gets built, nothing more.
FROM golang:1.26.6-trixie
COPY . /src

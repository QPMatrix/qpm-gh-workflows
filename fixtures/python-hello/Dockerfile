# syntax=docker/dockerfile:1
# Minimal fixture image for python-gate.yml's self-test `image` job —
# proves a Dockerfile in a caller repo actually gets built, nothing more.
FROM python:3.14.6-slim-trixie
COPY . /src
